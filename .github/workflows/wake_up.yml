name: Mobile-One-Click-Start

on:
  workflow_dispatch: # 手机 GitHub App 手动触发

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - name: Start Codespace
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🦞 启动 Codespace..."
          gh codespace start -R kexinzhang3-lab/openclaw || echo "Codespace 可能已在运行"
          echo "✅ 完成"
